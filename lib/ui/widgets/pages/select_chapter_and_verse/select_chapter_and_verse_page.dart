import 'package:bible_depth/data/bible/bible.dart';
import 'package:flutter/material.dart';

class SelectChapterAndVersePage extends StatelessWidget {
  Book book;
  SelectChapterAndVersePage({super.key, required this.book});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Placeholder(),
    );
  }
}
